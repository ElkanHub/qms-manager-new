import type { NextConfig } from "next";

// ponytail: default config is enough for the foundation; add options when a real need appears.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
